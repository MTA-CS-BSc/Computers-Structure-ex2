/**
 * Nor gate: out = 1 if {a==0 and b==0}, 0 otherwise
 */

CHIP Nor {
    IN a, b;
    OUT out;

    PARTS:
    Nand(a=a, b=a, out=not_a);
    Nand(a=b, b=b, out=not_b);
    Nand(a=not_a, b=not_b, out=a_or_b);
    Nand(a=a_or_b, b=a_or_b, out=out);
}
